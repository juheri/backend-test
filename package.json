{
  "name": "backend_test",
  "version": "1.0.0",
  "description": "project backend test",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run prisma-generate && nodemon index.js",
    "start": "npm run prisma-generate && nodemon index.js",
    "prisma-generate": "npx prisma generate",
    "prisma-db-push": "npx prisma db push",
    "seed": "node prisma/seed.js"
  },
  "keywords": [
    "backend"
  ],
  "author": "juheri",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.0.2"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  }
}
